#pragma once
#include<iostream>
#include<string>
using namespace std;


//===========PERSON HIERCHY====================
class Person          
{
private:
	string name;

public:
	Person(string name=" ");
	string getName() const;    //no setName bcs we dont need to change like the employees and stuff.
						 //once name is declared it can stay the same
};
class Employee : public Person
{
private:
	int ID;

public:
	Employee(string name=" ", int ID=0);
	int getID() const;    //making it const so it becomes read only

};
class Manager : public Employee
{
private:
	int bonus;   //added this so like I can show the hierchy is set up properly and that manager
				//has something special

public:
	Manager(string name=" ", int ID=0, int bonus=0);
	int getBonus() const;			//getters needed for cross class usage
};


//===========CLASSES FOR COMPOSITION====================
class Coordinates
{
private:
	double longitude, latitude;
public:
	Coordinates(double longitude=0.0, double latitude=0.0);
	double getLongitude() const;
	double getLatitude() const;
};

class Analytics          //this class is defining the behaviour of one array
{
private:
	double* data;
	int size;

public:
	Analytics(int size=24); //change this value in the constructor to add or remove months
	int getSize() const;     //for getting size to use in loops
	~Analytics();        //bcs dynamic allocation and shyt
	Analytics(const Analytics& other);   //deep copying array. Will use it later to set array=0 to
	                                  //avoid garbage values

	double& operator[](int index);   //overloading operator to use it like an array.
									 //we use & to make it both read and write
	
	double getTotal() const;    //profit calculations
};

class Store
{
private:
	string storeName;
	int storeID;
	Coordinates location;  

	Analytics sales;
	Analytics costs;
	Analytics customers;

	Manager manager;
	Employee* staff;
	int staffCount;

public:										 //location calling the default	constructor of Coordinates											
	Store( Coordinates location=Coordinates(), string name = " ", int ID = 0, int staffCount = 0);  
	Store(const Store& other);						//deep copies store
	~Store();

	Store& operator=(const Store& other);			//copies values across stores

	bool operator==(const Store& other) const;		//comparison b/w stores

	double CalculateProfit() const;
};
